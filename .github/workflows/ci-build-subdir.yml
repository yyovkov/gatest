name: TEST build subdir

on:
  workflow_dispatch:
    paths:
      - images/**

jobs:
  build-subdirs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Test ls
        run: ls -l

